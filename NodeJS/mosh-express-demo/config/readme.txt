Configuration folder,
    - Specifies settings specific to certain environments

1. custom-environment-variables.json
	- reads custom environment variables

2. Other .json
	- referenced depending on the environment specified i.e app.get('env')